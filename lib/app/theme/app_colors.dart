import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color backgroundDark = Color(0xFF121212);
  static const Color cardDark = Color(0xFF1E1E1E);
  static const Color cardElevated = Color(0xFF2C2C2C);

  // Accents (Pharmacy/Medical feel)
  static const Color primaryTeal = Color(0xFF00BFA5);
  static const Color primaryTealDark = Color(0xFF00897B);

  // Status Colors
  static const Color statusSafe = Color(0xFF4CAF50); // Green
  static const Color statusWarning = Color(0xFFFFB300); // Orange/Yellow
  static const Color statusCritical = Color(0xFFE53935); // Red

  // Typography
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xB3FFFFFF); // 70% white
  static const Color textMuted = Color(0x80FFFFFF); // 50% white

  // Borders & Dividers
  static const Color divider = Color(0xFF333333);
}
